# Defaults for g5kapi initscript
# sourced by /etc/init.d/g5kapi
# installed at /etc/default/g5kapi by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS="server -d --pid $PIDFILE"
